import { ReplacecolorDirective } from './replacecolor.directive';

describe('ReplacecolorDirective', () => {
  it('should create an instance', () => {
    //const directive = new ReplacecolorDirective();
   // (directive).toBeTruthy();
  });
});
